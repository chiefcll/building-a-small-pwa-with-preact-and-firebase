{
  "name": "app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "eslint src && preact test",
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "preact build && preact serve",
    "dev": "preact watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "devDependencies": {
    "eslint": "^5.12",
    "eslint-config-synacor": "^3.0",
    "firebase": "^5.7",
    "if-env": "^1.0.4",
    "node-sass": "^4.11.0",
    "preact-cli": "^2.2",
    "sass-loader": "^7.1"
  },
  "dependencies": {
    "lodash": "^4.17.11",
    "preact": "^8.4.2",
    "preact-compat": "^3.18.4",
    "preact-router": "^2.6.1"
  }
}
